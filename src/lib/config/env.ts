/**
 * Centralised environment configuration.
 * Do not access process.env in features or components — use this file only.
 * See ENGINEERING_RUELS.md and TECH_STACK.md.
 */

// TODO: Add API base URL, auth URL, keys when needed.
// Example: export const apiBaseUrl = process.env.NEXT_PUBLIC_API_BASE_URL ?? "";

export const env = {
  // Placeholder for env-driven config when .env files are introduced
} as const;
