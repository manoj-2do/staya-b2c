/**
 * Location search — re-export from shared types so feature code can keep
 * importing from this file. Source of truth: @/lib/types/locationSearch.
 */
export {
  type LocationSearchCoordinates,
  LocationType,
  type LocationSearchResult,
  type LocationSearchResponse,
} from "@/lib/types/locationSearch";
