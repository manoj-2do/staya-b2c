/**
 * Central API paths — used by frontend to call our app's API routes.
 * Keep in sync with backend API route handlers.
 */

export const apiPaths = {
  hotelSearch: "/api/hotel-search",
} as const;
